# NOTE: /api/node/config and /api/node/config/save routes have been consolidated
# into api_routes.py (registered first in meshtastic_dashboard.py).
# This file remains as a compatibility stub — the router is empty.

from fastapi import APIRouter
router = APIRouter()


